import { StyleSheet } from "react-native";

export const itemsSectionStyles = StyleSheet.create({
  card: { overflow: "hidden" },
  clearBtn: {
    width: "100%",
    borderRadius: 14,
    height: 46,
  },
});
