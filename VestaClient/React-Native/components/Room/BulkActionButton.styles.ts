import { StyleSheet } from "react-native";

export const bulkActionStyles = StyleSheet.create({
  btn: {
    flex: 1,
    height: 38,
    borderRadius: 14,
    borderWidth: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  text: {
    fontSize: 12,
    fontWeight: "900",
  },
});
