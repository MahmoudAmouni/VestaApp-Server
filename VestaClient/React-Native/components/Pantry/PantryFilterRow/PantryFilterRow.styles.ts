import { StyleSheet } from "react-native";

export const pantryFilterRowStyles = StyleSheet.create({
  row: {
    gap: 8,
    paddingRight: 8,
  },
  pill: {
    borderWidth: 1,
    borderRadius: 999,
    paddingHorizontal: 12,
    paddingVertical: 7,
    alignItems: "center",
    justifyContent: "center",
  },
  pillText: {
    fontSize: 12,
    fontWeight: "900",
  },
});
