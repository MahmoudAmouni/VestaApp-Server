import { StyleSheet } from "react-native";

export const recipesSectionStyles = StyleSheet.create({
  wrap: {
    gap: 12,
  },
  title: {
    fontSize: 16,
    fontWeight: "900",
    paddingHorizontal: 2,
  },
  list: {
    gap: 12,
  },
});
