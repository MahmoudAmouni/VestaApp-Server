import { StyleSheet } from "react-native";

export const shoppingHeroStyles = StyleSheet.create({
  btnRow: {
    flexDirection: "row",
    gap: 10,
    marginTop: 10,
  },
  heroBtn: {
    flex: 1,
    height: 40,
    borderRadius: 14,
  },
});
