import { StyleSheet } from "react-native";

export const descriptionSectionStyles = StyleSheet.create({
  wrap: { gap: 10 },
  h: { fontSize: 16, fontWeight: "900" },
  body: { fontSize: 12, fontWeight: "700", lineHeight: 16 },
});
