import { StyleSheet } from "react-native";

export const roomsHeroStyles = StyleSheet.create({
  top: {
    gap: 6,
  },
  title: {
    fontSize: 18,
    fontWeight: "900",
    letterSpacing: -0.25,
  },
  sub: {
    fontSize: 12,
    fontWeight: "700",
    lineHeight: 16,
  },
  grid: {
    marginTop: 12,
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 10,
  },
});
